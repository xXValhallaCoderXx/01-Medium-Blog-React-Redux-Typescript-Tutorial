export interface Valkyrie {
    entity: "valkyrie";
    saved: number
}

export interface Einherjar {
    entity: "einherjar";
    slain: number
}

export interface Aesir {
    entity: "aesir";
    power: string
}